# Serve sitemaps and robots as static files (must come before SPA fallback)
/sitemap.xml    /sitemap.xml    200
/sitemap-partners.xml    /sitemap-partners.xml    200
/robots.txt    /robots.txt    200
# SPA fallback
/*    /index.html    200
